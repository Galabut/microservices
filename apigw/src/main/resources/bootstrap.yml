spring:
  application:
    name: ApiGw
#  rabbitmq:
#      host: localhost
#      port: 5672
#      username: guest
#      password: guest

server:
  port: 8080

eureka:
  client:
    register-with-eureka: true

zuul:
  ignoredServices: '*'
  routes:
    clients:
      serviceId: Table
      path: /tables
    bills:
      serviceId: Bill
      path: /bill